/**
 * ${domain} Domain - Convex Schema
 * TODO: Define domain-specific schema
 */
import { defineTable } from 'convex/server';
import { v } from 'convex/values';

// Placeholder - replace with actual schema
export const schema = {};
