/**
 * seasons Domain Tests
 */
import { describe, it, expect } from 'vitest';

describe('seasons', () => {
  it('should export types', () => {
    // TODO: Add domain tests
    expect(true).toBe(true);
  });
});
