/**
 * listings Domain Tests
 */
import { describe, it, expect } from 'vitest';

describe('listings', () => {
  it('should export types', () => {
    // TODO: Add domain tests
    expect(true).toBe(true);
  });
});
